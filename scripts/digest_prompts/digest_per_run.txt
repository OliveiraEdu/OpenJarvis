WRITE THE DAILY DIGEST ENTRY FOR ${SLUG}. Topic: ${TOPIC}. This is a summarization task, not new research: do NOT search the web and do NOT use the calculator tool. Read ${REPORT} and ${NUMBERS} with file_read, then write ONE small file ${DIGEST_FILE} with a single file_write (path=${DIGEST_FILE}, mode='write', create_dirs=true, content under 1500 characters) containing EXACTLY these line types, in any order, nothing else, nothing missing: HOOK: <one short sentence capturing the single most notable or novel thing about this discovery, max 140 characters>; NOVELTY: <what is genuinely new or distinctive here — the discovery angle, a surprising capability, a design or business decision — max 200 characters, 1 to 2 lines>; SPEC: <one concrete capability or specification fact from ${REPORT} — e.g. parameters, context window, hardware, licensing, pricing — max 200 characters, 0 to 2 lines>; SOURCE: <url> — 1 to 3 lines, each line MUST start with the literal prefix 'SOURCE: ' (never a bare URL on its own line) and each url MUST be copied VERBATIM from the Sources & References section of ${REPORT} (never invent, shorten, or guess a url). Rules: focus on what is NOVEL about the discovery — not market-size or CAGR projections, which belong in the appendix, not the entry; every claim in HOOK, NOVELTY, and SPEC must state facts written in ${REPORT} (report-grounding) — never invent capabilities, numbers, dates, or license terms; every figure you use must be copied VERBATIM character-for-character from ${NUMBERS} or from ${REPORT} — never rounded, truncated, restated, or recomputed (write 13.066727193702121, never 13.07%); if ${REPORT} starts with a '> **UNVERIFIED**' banner or contains a PARTIAL REPORT note, keep your entry factual, do not claim figures are verified, and note the limitation inside the entry; every url must come from ${REPORT}. Do not add headings, markdown emphasis, or prose outside the line types. When the file is written, reply with just 'digest done' and the path.
