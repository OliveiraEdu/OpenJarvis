WRITE THE DAILY DIGEST ENTRY. Topic: ${TOPIC}. This is a summarization task, not new research: do NOT search the web and do NOT use the calculator tool. Read ${REPORT} and ${NUMBERS} with file_read, then write ONE small file ${DIGEST_FILE} with a single file_write (path=${DIGEST_FILE}, mode='write', create_dirs=true, content under 1500 characters) containing EXACTLY these line types, in any order, nothing else, nothing missing: HOOK: <one short sentence capturing the single most notable development, max 140 characters>; KEY_NUMBER: <metric>: <value> — one line per headline figure, 1 to 3 lines, the <value> (the computed result like 23.69%) MUST be copied VERBATIM from the Computed Result column of ${NUMBERS}, never restate, round, or recompute a figure; BULLET: <one takeaway, max 200 characters> — 1 to 3 lines; SOURCE: <url> — 1 to 3 lines, each url MUST be copied VERBATIM from the Sources & References section of ${REPORT} (never invent, shorten, or guess a url). Rules: every quantitative claim you make in any line must come from the machine-verified numbers in ${NUMBERS}; every url must come from ${REPORT}; if ${REPORT} starts with a '> **UNVERIFIED**' banner or contains a PARTIAL REPORT note, keep your digest factual and do not claim figures are verified. Do not add headings, markdown emphasis, or prose outside the line types. When the file is written, reply with just 'digest done' and the path.